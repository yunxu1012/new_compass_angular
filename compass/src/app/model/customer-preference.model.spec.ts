import { CustomerPreference } from './customer-preference.model';

describe('CustomerPreference', () => {
  it('should create an instance', () => {
    expect(new CustomerPreference()).toBeTruthy();
  });
});
